import discord
from discord import app_commands
from discord.ext import commands
from config import BOT_DISPLAY_NAME
from utils.replies import reply_ephemeral
from utils.embeds import EMBED_COLOR, embed_footer, howlbert_embed
CREDITS_TEXT = f"**{BOT_DISPLAY_NAME}** was built for wolf rp on discord.\n\n**inspired by** (not affiliated):\n• [wolvden](https://www.wolvden.com/); wolf sim & pack life\n• [warrior cats](https://warriors.fandom.com/wiki/main_page); den life, herbs, and cough lore\n• [the whispering wild](https://www.roblox.com/games/1625049462/the-whispering-wild-wolf-rpg); wolf rpg on roblox\n• [unbelievaboat](https://unbelievaboat.com/); economy & shop bots\n• quest bot; quest board workflows\n\nrules & herbs follow **basil's wolf ttrpg** homebrew."
HELP_TOPICS = {'overview': (f'{BOT_DISPLAY_NAME}; wolf rp', f"**{BOT_DISPLAY_NAME}** tracks your wolf, bones, quests, pack, and legacy across the wild.\n\n**quick start**\n1. `/register`; create your wolf; pick a great pack or walk as a lone wolf\n2. `/bones action:hunt` and `/bones action:work`; earn 🦴 bones (`/bones action:daily` draws from pack treasury if you're in a great pack)\n3. quests on the den board accept and complete automatically; check `/checklist`\n4. `/profile`; view your sheet (`sheet:true` for lore) · `/checklist` for setup and daily todos\n5. `/terms` or `/help topic:terms`; wolf tongue glossary (fresh-kill, newgrowth, insults)\n\n`/ping`; bot health check\n\nuse `/help topic:getting-started` for your first week.\nuse `/help topic:<category>` for more.\ntype `/` in chat; discord autocompletes all commands."), 'getting-started': ('getting started', '**day 1**; `/register` → `/bones action:hunt` or `action:work` → `/profile` → `/checklist`\n**pack life**; `/bones action:daily` · `/pack treasury` · `/playpen action:socialize` · `/pack stash`\n**juveniles (6 to 24 moons)**; hunt for practice; first kill earns **blooding**; then `/role action:event`\n**life**; `/courtship action:court` → `action:mate` (winter) · `action:pregnancy` · `/pupcare action:birth names:…` · `action:feed` · `action:save` · `action:list`\n**adopt**; bonded mates use `/pupcare action:adopt`; other players accept via button or `respond:`\n**survival**; `/eat` from `/food`, `/drink` at the creek; watch hunger/hydration on `/vitals action:condition`; `/medic action:deathsaves` at 0 hp\n**help**; `/help topic:hunting` · `life` · `pack` · `world`'), 'profile': ('profile', "`/register`; create wolf; optional **starting_age** and **genetic** (blind, half_blind, deaf, missing_leg, no_tail, …)\n`/wolves`; list your wolves\n`/switchwolf`; change active wolf\n`/setfaction`; join/switch great packs or go loner (🦴 500 to switch between packs)\n`/rename`; change wolf name\n`/profile sheet:true`; view wolf sheet with lore (attributes, hp, skills)\n`/checklist`; pending setup todos (proxy, ic location, …) and **today** dailies (hunt, stipend, howl, …)\n`/rpg action:setstats`; assign attributes within role range\nroles include **pup**, **juvenile**, apprentice ranks (**hunter/medic/scout/diplomat/caretaker/forager apprentice**), and full adult roles.\npups cannot hunt or mate; juveniles can hunt but not mate. **apprentice** roles are valid from juvenile age; stepping stones to full rank.\nuse `/help topic:roles` for mechanical role perks.\nlife stages: **under 6 moons** (pup) · **6 to 24 moons** (juvenile) · **24 to 59 moons** (adult) · **60+ moons** (elder). pups and juveniles are still growing; physical (str/dex/con) checks take a fading penalty (**−2** under 12 moons, **−1** through juvenile, gone at adulthood), the mirror of the very-old elder drift.\neach `/rollover` is **one sunrise** (hunger/hydration decay, long rest). with **lunar birth aging** (default), wolves gain **+1 moon** only when the real sky matches their birth phase (**new**, **half**, or **full**). `/world action:time` shows tonight's moon.\nset `auto_rollover_enabled=true` in `.env` for a daily sunrise at `rollover_hour` in `rollover_timezone` (default us eastern). if the bot was offline, missed sunrises catch up on the next startup (up to 31 days).\nhunger **−12** and hydration **−14** each sunrise; below **30** each adds **+1 exhaustion** (both low = +2). low mood below **30** also adds **+1 exhaustion**. exhaustion **6 = death**. every field, social, or den action also spends **energy** (`/vitals action:condition`); acting at **0** energy adds exhaustion and costs mood. energy refills each sunrise, from `/vitals action:rest`, and via a slow real-time drip while idle, all scaled by hunger. at **0** hunger or hydration, wolves **collapse** into dying.\nwolves at **120 moons** pass from **old age** on the next sunrise.\n**standing** can go negative; at **−5** you are cast out as a **loner** (an **alpha** at **−5** triggers the **rite of the broken canine** instead; `/pack brokenrite`).\n`/vitals action:condition`; injuries, disease, exhaustion, mood, hunger & hydration (bleeding/infection progress each `/rollover`); **3-step treatment plan** embed\n**disease**; wolvden illnesses + warriors cough stages (`/vitals action:condition` · `/medic action:treat`):\n• **whitecough** = **green-cough (mild)** (`cough:mild`); see `/terms`\n• **cough** (green/white → black → red); rare **green-cough** mold from rotten meat; **14%** den spread/rollover\n• **diarrhea**; rotting meat or rolling in filth (`/playpen`, awkward `/playpen action:socialize`, bad explore)\n• **influenza** (50% den spread); failed blizzard, freezing rain, or deep snow `/world action:hazard`\n• **fleas / insect stings / poison ivy**; hunt, forage, explore, verge edge-forage; **nettle welts** when gathering stinging nettle; **mild venom** from creek fishing and snake ambushes (mistmoor/silverrush)\n• **snakes, skinks, spiders**; ambush on hunt/explore/patrol (pack-weighted); venomous bites in combat; **fear of reptiles and insects** (sypha) hurts your rolls vs them\n• **hepatitis / distemper**; carrion and `/field action:scavenge`; **mange**; mangy den sites\n• **pox**; den filth; hits pups harder; spreads by den contact\n• **distemper**; sick canid carrion, hearth-hound bites, den contact\n• **yellowcough**; epidemic plague; **45%** den spread; lethal without **mullein** or **lungwort**\n• **redscratch**; mating sti; blocks conception; **lavender** or **chervil**\n• **rot-lung** (mistmoor); fever → wheeze → necrosis; **42%** den spread; **marsh-mallow**, **feverfew**, **mullein**, **lungwort**, or **belly-rip fungus** (necrosis)\n• **milk-fever**; eclampsia 1 to 3 sunrises after `/pupcare action:birth` (peak nursing); **parsley**, **saffron**, or **feverfew**\n• **nursing**; mothers **`/pupcare action:feed`** each sunrise until pups reach **6 moons**; **honey** in inventory auto-sweetens the meal (**+10** hunger); unfed pups lose extra hunger at `/rollover`\n• **lone mothers**; no pack caretaker; nurse daily or **`/medic action:treat`** with **honey** on the pup\n• **shaking-sickness** (mistmoor); belly-rip bad water; tremors → hemorrhage; **sweet sedge**, **yarrow**, or **cobwebs**\n**chronic illness**; progressive, often elder-onset (`/vitals action:condition` · `/medic action:treat`):\n• **rabies**; feral hearth-hound / wolf bites; incubation → frenzy → death; **boneset** or **goldenrod** slow early stages (no cure)\n• **wasting sickness**; carrion or old age; hunger drain → cachexia; **borage** or **parsley**\n• **river rot** (silverrush only); sewage-tainted creek water; fouled gut → bloody flux → failing; no known cure, fatal without den care\n• **growth-sickness**; rare in elders who outlive the wild; hidden lump → spreading; **mullein** or **lungwort**\n**paralysis**; **spinal injury** (temporary, spine bite) or **paralyzed** (permanent); comfrey + bindweed splint; blocks hunt/patrol/explore\n**mental illness**; insomnia, anxiety, grief, delirium, pack madness, obsession, night terrors, chronic stress, eating distress; calming herbs (**chamomile**, **valerian**, **poppy**, **lavender**, **passionflower**, **skullcap**) have real cure/buff paths via `/medic action:treat`\n**mental degeneration**; dementia (forgetful → lost) and feral shift → **mind-fracture** (unsentient; **rp fantasy**); **chamomile** or **dried skullcap** may slow decline; unsentient wolves keep `/vitals`, `/eat`, `/drink` but lose field commands\n**restricted herbs**; poison plants (bloodroot, foxglove, wolfsbane, …): **−4** if caught using; hoarding risks **−3 if caught**; `/herbs action:turnin` (**+1 standing**, **10🦴** from pack treasury when funded)\nillnesses can pass during **`/mate`** (close contact): respiratory diseases most easily, then fleas/mange/pox; diarrhea at low risk. den rollover and groom/socialize still apply.\ngroom & socialize can pass illness immediately; packmates catch it each `/rollover`.\n`/medic action:quarantine`; medics, alpha, or advisor isolate sick wolves (blocks spread & activities)\n`/vitals action:rest`; short rest clears strain (+ optional comfrey heal); long rest restores **3 hp**, **−2 exhaustion**, and mood\n`/medic action:treat herb:`; inventory herb **or** forage `stack:id` (**medics**: unlimited; **`patient:`** treats packmate from your bag)\n`/herbs action:store mode:`; pack herb store; anyone: **deposit** / **depositall** (forage bag + inventory); medics/foragers: **withdraw**\n`/herbs action:turnin`; any wolf: hand **restricted poison** herbs to the healers' den (**+1 standing**)\n`/medic action:ritual` · `action:naming` · `action:lay_to_rest` · `action:swim`; spirit rites & swim therapy\n`/herbs action:prepare`; dry, poultice, juice, tea, gargle, ointment, sap, rub, eaten raw/cooked, simmered milk on a forage stack or inventory herb (dry first, then tea/rub/juice/poultice/etc from the dried herb; sweeten a tea or gargle)\n`/herbs action:dryall`; dry every fresh forage stack, `herb_*` in `/bones action:inventory`, and fresh den store stacks\n`/bones action:inventory`; fresh & prepared herbs (foraged; rot if not dried)\n`/herbs action:guide`; paginated herb guide (gathering, effects, treat keys)\n`/rpg action:delete confirm:delete`; remove active wolf (keeps prestige)"), 'economy': ('economy', '`/bones action:balance`; your bones\n`/bones action:daily`; sunrise stipend from **pack treasury** (25🦴 base + prestige; loners ineligible)\n`/bones action:give`; gift bones (`@wolf` or `own_wolf:`)\n`/bones action:giveitem`; gift herbs or shop items from `/bones action:inventory`\n`/trade offer`; propose items/bones; recipient accepts or declines\n`/trade cancel`; cancel your pending offers\n`/bones action:leaderboard`; richest wolves\n`/bones action:shop` · `action:buy` · `action:sell` · `action:inventory` · `action:use`; trading post\n`/bones action:sell item:stack:id`; sell a forage herb stack for bones (rarity-based; poison herbs use turn-in)\n`/bones action:work` · `/crime` (petty crime) · `/crime target: raid_type:` (rival treasury/food/herbs/toys; pack-specific catch/loot; rival **watch** raises sniff odds; paranoia phases: **25%** unity risk on clean raid)\nshop: utility items plus **prey carcasses** (`prey_vole`, `prey_hare`, …) and **toys** (`toy_bone`, `toy_feather`, …); carcasses go to `/food`, toys to `/playpen action:toys`.\nalso: **vitality salve** (550🦴) · **lucky tooth** · **herb bundle** · **prey bundle** · **den charm** · **rabbit pelt** · **an extra paw** · **safe roll** · food/toys as `prey_*` / `toy_*` keys\n**vitality salve**; `/pupcare action:save name:<pup>` on the **same sunrise** a lethal-at-birth pup is born\n**revive** ($35 ko-fi); same wolf back from death · **reincarnation** ($28 ko-fi): new name, juvenile age, keep stats\nretire a wolf: `/rpg action:delete confirm:delete`'), 'hunting': ('hunting & foraging', f"all roles **1 hunt per sunrise** except **hunters** (**10**). weather and **season** affect bone payouts; check `/world action:weather` and `action:time`.\n\n`/bones action:hunt`; main hunt; carcass goes to **food hoard** (`/food`). a **lone** hunter rarely meets large prey (elk/moose); only a cornered, sick, or desperate one is takeable solo. call a pack hunt for the full large-prey rate.\n`/bones action:hunt collaborate:true`; **pack hunt**: call the den, packmates join via buttons (roles: leader/chaser/flank/scout/blocker), set out together (+bones per extra wolf; large prey & ambushes possible; party fights together; fresh-kill to `/preypile`)\n`/scout survey collaborate:true`; **scout border sweep**: scouts join via buttons, stealth sweep together (+bones per extra scout; ambushes possible; counts as survey)\n`/scout trail collaborate:true`; **pack trail**: scouts join via buttons, tracking sweep together (+bones per extra scout; prey to caller; ambushes possible; counts as trail)\n`/pack patrol collaborate:true`; **war patrol**: packmates join during a territory war (combined war points; counts as your daily patrol)\njuveniles (6 to 24 moons) earn **blooding** on first successful hunt (+8 mood, +1 standing)\n`/field action:track`; trail-age tracking dc (fresh dc 8 → faint dc 25); weather affects scent\n`/field action:fishing` · pack-specific fish & turtles; rain/dawn/night gate legendaries (`/world`) · `action:scavenge`; more carcasses\n`/field action:sniff` once per sunrise; rival wolves at **≤3** standing may trigger border skirmishes\n\n`/field action:mark territory:`; refresh your den's scent on shared borders (once/sunrise). over-marking rival-held ground costs **−2** pack standing and can open war at **0**.\n`/food`; view hoard · `/eat` · `/drink` (hourly) · `/salvage` rotting meat\ncombat kills (coyotes, cats, hearth-hounds, …) go to your hoard too. **wolf carcasses** are edible; private `/eat` always costs mood and may get you caught; `/preypile`, `/pack stash deposit`, or serving wolf meat at `/packlife feedall` **always** costs standing.\n`/preypile`; lay a **today's** fresh carcass at the den cache for the pack\n`/field action:forage`; gather herbs in pack territory (**full foragers** unlimited; apprentices once/sunrise; harder in leaf-bare; crit → rare herb + season blurb)\n`/field action:verge`; **thunderpath** or **twoleg fence-line** herbs (once/sunrise; riskier)\n`/checklist`; what's ready this sunrise"), 'wolvden': ('explore & hoard', 'inspired by [wolvden](https://www.wolvden.com/); explore, hoard, mood, and trade.\n\n**food hoard**; `/food` `/eat` `/drink` `/bury` `/preypile` `/salvage` (bury: optional **lavender/rosemary/meadowsweet/mint** masks death-scent) · **hunger & hydration** slip each sunrise\n**hoard**; `/hoarding action:hoard` · `action:shred` toys → remnants · `action:craft` (bone toy **8**, stick bundle **6**)\n**explore**; `/explore` (dig · follow · investigate; **scouts: unlimited**)\n**scout**; `/scout rescout` · `/scout survey` · `/scout trail` · `collaborate:true` on survey/trail for pack parties\n**amusement**; `/playpen action:toys` · `action:play` (**once per sunrise** per wolf) · `action:toystore` · `action:playall` (alpha; also counts as play) · `/hoarding action:gift`\n**pack**; `/playpen action:socialize` · `action:groom` · `/pack stash` · `/packlife action:feedall` · `action:drinkall` (alpha)\n**bonds**; `/bonds` friendships, rivalries, kin, mentors · found families (`/playpen action:socialize` & `action:groom` deepen ties). canonical lore bonds seed automatically when both wolves are registered.\n**creek**; `/drink` once per hour (+hydration, +hunger, small heal)\n**survival**; at **0** hunger or hydration wolves **collapse** (death saves via `/medic action:deathsaves`)\n**sniff**; `/field action:sniff` wind-read once per sunrise\n**wolvden parity** (ported vs different):\n✓ food hoard · hunger/hydration/mood · explore · diseases · nursing · genetics\n✓ **late pregnancy den rest**; final third of gestation blocks hunt, patrol, explore, etc.\n✓ **pack hunt roles**; leader/chaser/flank/scout/blocker auto-assigned; full five-role party **+8%** chemistry\n✓ **spring breeding pair**; mated male + pregnant female in party adds hunt drive bonus\n✓ **hunter hunt cap**; **10 hunts per sunrise** (wolvden parity); other roles **1** hunt per sunrise\n\n**patron**; `/patron` invites, boost & donor status · kickstarter backer badge · `/redeem` gift codes'), 'unbelievaboat': ('unbelievaboat-style economy', 'inspired by [unbelievaboat](https://unbelievaboat.com/) shop bots: bones, shop, sell-back, and daily payouts.\n\n**balance**; `/bones action:balance` · `action:leaderboard`\n**income**; `action:hunt` (**hunter** 10/sunrise, others 1) · `action:work` · `/crime` (once per sunrise each)\n**pack pay**; `/bones action:daily` draws from **pack treasury** (great pack members only)\n**shop**; `action:shop` · `action:buy` prey (`prey_vole`) and toys (`toy_bone`) into `/food` and `/playpen action:toys`\n**sell**; `action:sell item:herb_arnica` (inventory herbs) or **`item:stack:id`** (forage bag stacks)\n**poison bounty**; `/herbs action:turnin` pays **10🦴** from treasury + standing (restricted herbs only)\n**trade**; `/trade offer` · `/bones action:give` · `action:giveitem`\n_not affiliated with unbelievaboat; similar command feel only._'), 'whispering': ('whispering wild', 'inspired by [the whispering wild](https://www.roblox.com/games/1625049462/the-whispering-wild-wolf-rpg) wolf rpg on roblox; fog spirits and swamp unease.\n\n**fog & storm**; `/field action:sniff` in **fog**, **rain**, or **storm** may bring spirit whispers\n**mistmoor & drown-sick**; higher chance of **anxiety** from whispers on the wind\n**spirit curse**; rare in fog if whispers turn cruel; **−1** spiritual checks until broken (wolfsbane, swamp milkweed, cleansing ritual, `/skills` spirit_cleanse)\n**pack madness**; mental illness from paranoia and den stress; treat via calming herbs\n**whispering rot**; guard role event in mistmoor fog (betrayal temptation)\n_not affiliated with the roblox game; shared wolf-rp mood only._'), 'quests': ('quests', 'quests are fully automatic now; no command needed.\nden board and daily quests accept themselves each rollover; objectives track automatically when you hunt, patrol, deposit, explore, etc.; rewards (bones, standing, xp) grant the moment an objective finishes.\n`/checklist`; see your active quests and progress\n`/journal`; see completed quest history\n`/role action:quests`; quests only your **role** can take (also auto-accepted)\n`/role action:event`; once-per-rollover role scene (skill check + reward)'), 'roles': ('role features', "your **role** on `/profile` grants mechanical perks. purchased extras show as **bonus feature**.\n\n**alpha**; **commanding howl**: `/howl` grants packmates advantage on their next skill check or attack.\n**advisor (beta)**; **blood oath**: advantage on one charisma check per sunrise (role events, forage, `/rpg action:roll`, cat pacts, etc.).\n**guard**; **defender's resolve**: attackers have disadvantage when striking anyone else in the same combat.\n**hunter**; **killer's instinct**: +1d6 damage vs prone/pinned foes; **10 hunts per sunrise**.\n**medic**; **green tongue**: unlimited `/medic action:treat` and comfrey heals per sunrise; packmates with disease or injuries appear on the medic's `/checklist` each sunrise.\n**scout**; **unseen paw**: hidden in fog/mist on successful `/scout survey` or `/field action:sniff`; attackers have disadvantage against hidden scouts in combat.\n**forager**; **nose of the land**: auto-herb each sunrise; **full foragers** unlimited forage (apprentices once/sunrise).\n**caretaker**; **soothing lick**: `/playpen action:groom` clears **distressed** and gives **+12 mood** when target mood is below 30.\n**diplomat**; **silver tongue**: `/rpg action:roll use_role_reroll:true` on failed charisma checks.\n**elder**; **wisdom of years**: `/rpg action:roll use_role_reroll:true` on any failed skill check.\n**drown-sick**; +2 perception in fog/swamp; −35% hunt bones; `/role action:prophecy`.\n**rogue**; no `/bones action:daily`; stealth advantage crossing borders.\n**bog-born**; survival/herblore advantage in mistmoor swamp.\n\nspend xp at `/advance action:spend purchase:role_feature` to **request** a bonus role feature (10 xp; **requires admin approval** via `/wolfadmin approvefeature`)."), 'lore': ('maw faith & oracles', 'all four packs share the faith of **the maw**; the living ancestor whose body is the land.\n\n`/role action:prophecy`; drown-sick only: draw a cryptic prophecy (once per rollover)\n\n**spirit curse**; rare from belly-rip whispers in fog (mistmoor/drown-sick); **−1** on spiritual checks. lift with wolfsbane, swamp milkweed, medic cleansing, or `/skills category:spiritual check:spirit_cleanse`.\n\n_when the teeth drink the tears, when the fur chokes the belly…_'), 'pack': ('pack & warfare', "your **great pack**; greyspire, mistmoor, thistlehide, or silverrush.\nall wolves in the same great pack share treasury, tax, wars, and unity.\n**lone wolves** register as **loners**; apart from packs, not rogues (`/setfaction` to join a great pack).\n\n`/pack treasury` `/deposit` `/withdraw`; communal bones\n`/pack stash list` `/deposit` `/withdraw`; shared food reserve (rots slower)\n**hunters** depositing to `/pack stash` feed the healer den: **+1 unity** & **+1 standing** once per sunrise\n`/packlife action:feedall`; communal meal from reserve; **alpha** (once per pack per sunrise)\n`/packlife action:drinkall`; creek drink for whole den; **alpha** (once per pack per sunrise)\n`/playpen action:playall`; den romp mood boost; **alpha** (uses your toys or den toy store; marks **play** used for every denmate)\n`/playpen action:toystore mode:`; den toy store (list · deposit · depositall · withdraw)\n`/pack taxrate` `/settax`; hunt tax (alpha, 0 to 25%)\n`/pack territory`; territory map\n`/pack challenge territory`; declare war (alpha)\n`/pack brokenrite`; latest **rite of the broken canine** (leadership challenge)\n`/pack patrol` `/pack patrol collaborate:true` `/pack scout`; earn war points during conflict\n`/pack howl` `/pack share` `/pack aid`; diplomatic standing with rival great packs (**≥8** friendly: allied pack hunts · **≤3** hostile: sniff skirmishes & fresh-kill fights · **0** war: auto territory war when no conflict is active)\n`/pack resolvewar`; **alpha** or **diplomat** ends an active war and awards territory by score\n`/pack unity`; pack unity **−5 to 10**; low unity hurts hunts; **−5 dissolves** the den\n`/howl`; once per sunrise; raises unity (**alpha** or **beta/advisor** rally when unity ≤ 0). **alpha** howls grant **commanding howl**; packmates gain advantage on their next check or attack.\n`/sign`; body/visual language (unlimited; repeat signs on the same partner pay out less mood, see `/help topic:roleplay`); **alert · rally · play · submit · soothe · threaten · freeze · track · greet · grieve · challenge**. **mute** wolves can't `/howl`, so their **rally** sign builds extra unity instead. denmates answer with `/sign signal:read`.\n`/pack relations` `/relation`; rival den standing\n`/pack audit` after a treasury raid (stealth check; recovers **40%** of stolen bones) · `/pack accuse` (alpha/beta names a rival; correct clawback **30%** + **−2** standing)\nden rhythm: if **≥50%** of the pack hunts, howls, and socializes the same sunrise → **+1 unity** next rollover\n`/pact`; **warrior cats** clan treaties (alpha or **diplomat**): thunderclan, shadowclan, windclan, riverclan\n`action:receive`; collect **clan patrol** goods at the scent-line (trust 35+, once/sunrise)\n`action:trade`; barter **duplicates** for clan prey, herbs, toys + trust\n`/trade duplicates`; give all extras to another wolf (once per sunrise)\n`/pack tradepack`; cross-pack duplicate trade (+standing when relations allow)\n\nlake territory · warrior patrols, deputies, rogues, loners, kittypets · seasonal **gathering** at fourtrees (unity/standing when treaties active)\ntribute from treasury · charisma parley · fewer `/field action:sniff` border fights while active\n\nbreaking a pact or killing an allied **warrior patrol** costs **trust**, **unity**, and **standing**"), 'world': ('world & rollover', "`/world action:time`; sunrise, season, time of day\n`/setseason`; **admin**; jump season\n`/world action:weather` · `action:forecast`; current weather and hunt effects\n`/checklist`; what's ready this sunrise\n`/world action:travel` · `action:encounter` · `action:omen`; travel hazards, border encounters (**loot/damage/combat**), **starclan** omens\n`territory:twolegplace`; thunderpath monsters, twoleg nests, pet dogs\n\n`/medic action:sacred`: medic half-moon visit; ancestors speak + **+2 standing**, **+5 mood**, **+1 pack unity**, next medicine/herblore **+2** (miss: **−2** standing)\n`/rollover`; advance one sunrise (admin)\n`/world action:hazard`; weather hazard opposed roll (`/hazard` still works)\n`/terms` · `/help topic:terms`; wolf tongue glossary\n`/ping`; bot health check\n\none rollover = **one sunrise**. seasons: newgrowth, highsun, leaf-drop, leaf-bare."), 'terms': ('wolf tongue', f'`/terms topic:` or `/help topic:terms`; glossary used in **{BOT_DISPLAY_NAME}**\n• **overview**; quick map (sunrise, fresh-kill, loner vs rogue)\n• **basic**; fresh-kill, sharing tongues, thunderpath, twolegs…\n• **seasons**; newgrowth, moonhigh, quarter-moon…\n• **measurements**; pawstep, deer-length, tail-length…\n• **insults**; deerheart, carrion-breath, pup-brain…'), 'prestige': ('prestige & legacy', '`/prestige action:view`; your tier and bone bonus\n`/prestige action:require`; next tier requirements\n`/prestige action:bonus`; unlocked tiers\n`/prestige action:legacy`; legacy score and dynasty\n`/prestige action:retire`; add wolf to dynasty (+legacy)\n`/prestige action:halloffame`; top legacy scores\n\nhigher tiers grant permanent +% bones on hunts and daily. legacy persists across wolves on your account.'), 'rpg': ('rpg rolls (basil rules)', "**hp** = 10 + strength + survival (constitution score). example: 10 + 7(str) + 5(survival) = **22**.\nroll **1d20 + attribute modifier + character traits** (weaknesses subtract).\nfailed **skill** rolls (`/skills`, `/rpg action:roll skill:…`) build practice strain before traits slip.\nnat 1 = critical failure · nat 20 = critical success.\n\n`/rpg action:roll skill:tracking dc:15`; skill check\n`/rpg action:roll attribute:wisdom dc:12`; raw attribute check\n`/rpg action:roll use_safe_roll:true`; reroll a failed check with **safe roll** (not combat)\n`/rpg action:roll use_role_reroll:true`; **elder**/**diplomat** reroll on failure (once/sunrise)\n\n`/combat start` `/join` `/list` `/npc` `/begin` `/attack` `/maneuver` `/npcattack` `/yield` `/status` `/end`\n`/combat encounter`; random ambush (90 min cooldown; also triggers on hunt/explore)\nyielding may be **caught** (~35%); **−2 standing** if you're still in a den.\n`/combat npc`; add a predator, **clan cat**, hearth-hound, fox, badger, or reptile from the bestiary (not custom hp)\n`/combat hazard topic:`; two-legs, thunderpath, traps, fences, **fire fear**, wildfire; **rolled** (damage, injury, disease)\n`/combat guide topic:bestiary`; cats, foxes, badgers, reptiles, border patrols\n`/combat guide topic:`; vulnerable areas, stance, maneuvers, injuries, crits\n**injuries**; 1d10 on crit or dropping to 0 hp · **crit/fumble**; nat 20/1 rolls 1d4 extra\n**pin**; pin sets **prone**; pinning wolf shown as `pinning`. only **same size or larger** fighters can pin (cats cannot pin wolves). clan **cats** use maneuvers in npc fights (rakes, badger defence); no pinning wolves.\nafter `/combat begin`; **pick a target** from the dropdown, then **bite** / **claw** / **maneuver**\nseveral fights can run in one channel; each has a **fight #**. `/combat list` shows them; `/combat join` works mid-fight (rolls initiative). pass `encounter:` when several are open.\ntarget lock is saved; any combat message in the channel works after a bot restart\nplayer turn → target + **bite** / **claw** / **maneuver** · npc turn → **npc attack** menu or `/combat npcattack`\ndc tiers: easy 10 · moderate 15 · hard 20 · legendary 25\n\n`/skills`; run catalogued basil checks (tracking, stealth, social, survival, herb prep, …)\n`/skilllist`; reference dcs by category · freeform: `/rpg action:roll`"), 'skills': ('skill checks (basil catalog)', "**69** scripted checks from the basil rules; run with `/skills category:… check:…`.\n\n**categories:** tracking · stealth · howling · social · spiritual · survival · herb_prep · crafting · navigation\n\n`/skilllist category:tracking`; list dcs without rolling\n`/skills group:true`; pack group check (half must pass)\n`/skills helper:@wolf`; assisted check (helper dc 10 → your advantage)\n`/field action:track trail_age:…`; daily hunt alternative using tracking dcs\nopposed checks need `/skills opponent:` (packmate or rival wolf).\ntracking scent decay: rain/snow/wind raise dc; use `rained:true` when relevant.\nmoonhigh **night** adds **+2 dc** to tracking and stealth; half-light **+1**.\n**long-term injuries** (limp, scars, chronic pain) show on `/vitals action:condition`; cure with wolfsbane or swamp milkweed.\nfull basil reference: `docs/basil_rules.md`.\n\n**herb loop:** `/field action:forage` → fresh stack in `/bones action:inventory` → `/herbs action:prepare` or **`action:dryall`** (bag + `/bones action:inventory` + den store) → `/medic action:treat` with `stack:id`.\nstock the healers' den: `/herbs action:store mode:depositall`. fresh forage stacks rot after **1 sunrise** if not dried.\nshop items (`/bones action:inventory`) stay stable; dry or deposit forage herbs when ready.\n\n**herb prep checks** (`/skills category:herb_prep`); success promotes **fresh** bag herbs into **dried / poultice / tea / juice / ointment / …** stacks and grants timed buffs (pain relief, disease-save advantage, storage bonus). no fresh stacks → buffs still apply but nothing is stored. **`prep_taste_test`** is opposed (plant poison dc). **`prep_incomplete_antidote`** halves poison and stores a weak tea when herbs allow.\n\n**filth**; awkward `/playpen action:socialize`, toy play, or bad explore can contract **diarrhea** or **pox**. **burial scent mask** (rosemary, bury ritual) halves den filth rolls.\n\n**energy; why repeats tire you out**\nalmost nothing is a hard once-per-sunrise block (only **long rest** and the **daily stipend** are). every field, social, or den action spends **energy** (see it on `/vitals action:condition`). energy never refuses a command: acting at **0** still works, it just adds **exhaustion** and costs **mood** on top of the action. energy sits **above** the exhaustion / pain-exhaustion layer, not replacing it. it restores three ways, all coupled to **hunger** (a starving wolf recovers less): a big chunk each **sunrise** sleep, a smaller chunk from `/vitals action:rest`, and a slow **real-time drip** while you are idle during the day. regen also slows when the body is taxed; **pregnancy**, **nursing**, active **illness**, and **winter** cold all burn extra calories. separate from energy, **prey and forage** still pay a **shrinking share** on repeat gathering in one sunrise (over-worked land yields less; forage/verge climb the dc); **full foragers** are exempt from the forage climb."), 'life': ('life & advancement', "**death**; at 0 hp: `/medic action:deathsaves` (3 rounds dc 10/12/15). all pass → 1 hp. any fail → death.\n`/medic action:stabilize`; ally medicine dc 15; cobwebs auto-stabilize (**medics**; unlimited per sunrise).\n`/medic action:surgery`; **medic** or **medic apprentice** (+2 dc): stitch, set bone, extract, amputate (herbs from `/bones action:inventory` or `/bones action:inventory`; **stick** from patient or medic: **2 sticks** for set bone; optional `use_meadowsweet`, `use_loosestrife`, `use_plantain`, `use_poppy`, `use_rush_stalks`; optional `helper:` medic for assisted surgery; once per sunrise).\n`/medic action:observe`; apprentice/senior observe a case (quest progress; no surgery cooldown)\n`/medic action:dissect`; **medic apprentices** study a dead packmate (`deceased`/`own_deceased`) to learn anatomy: needs an **alpha** or **full medic** in the den to sanction it; once per sunrise, up to 3 lessons total; success deepens **medicine** skill (a slip risks infection); solemn work costs **−5 mood** and notifies the fallen wolf's player\n`/medic action:checkup`; **full medic only**; once-per-sunrise den scan (contagion, mind & spirit, bleeding, dying, herb bags; **~52%** catch poison hoarders); sick packmates appear individually on `/checklist` before rounds are done\n\n**xp**; +1 from daily, quest complete, and den chat (once per sunrise). `/advance action:spend` for +1 attribute, **+1 earned skill trait** (max +3 from play; stacks with lore traits on `/skills` checks); **failed skill rolls** build **practice strain** (close misses are nerves only); at **3 strain** you lose earned trait progress or gain a setback; success and `/rollover` rest ease strain; `purchase:role_feature` submits a bonus role-feature request (**admin approval**).\n`/character`; update birth sex, sexuality, maw belief, **combat size** (small / medium / large), or age.\n\n**mating**; `/courtship action:court` then `action:mate` (winter). `/courtship action:rival`; spring physical or vocal challenge for mating access. losing a **vocal** rival challenge blocks court **and** mate until next sunrise.\n**medics**; healer's code is not enforced by the bot; if caught courting/mating **−3 standing**. bearing or adopting pups **exiles the litter and the healer**.\ncross-player mating needs partner **accept** (button or `respond:`). failure **−4 mood** ( **−7** if hostile). mating **+12 mood** each.\n**inbreeding** is taboo, not blocked, and now carries a real biological cost: a kin litter surfaces hidden recessives (blindness, deafness, albinism) far more often, carries a heavier mutation load, is far likelier born with **inbreeding depression**, and runs a stillbirth risk. recessive traits pass silently through **carriers** and can resurface generations later.\n`/courtship action:pregnancy`; yours or your bonded mate's. `/pupcare action:birth names:…` · `action:feed` (mothers nurse; **caretakers** mash-feed pack pups once per sunrise) · `action:save` with **vitality salve** · `action:adopt` · `action:list`. juveniles: first hunt kill = **blooding**, then `/role action:event`."), 'roleplay': ('roleplay & proxying', "**speak as your wolf (tupperbox-style)**\n`/proxy set tag:h:text`; register a proxy tag (use `text` as the placeholder, e.g. `h:text`, `[text]`, `text-h`). then typing `h:hello` reposts as that wolf and deletes your message.\n`/proxy avatar`; upload an image or paste a url, then pan/zoom in the crop editor and **save**. `/proxy list`; see your tags. `/proxy clear`; remove a tag.\n`/proxy autoproxy mode:on`; proxy **all** your untagged messages as one wolf (start a line with `\\` to send as yourself once).\n`/proxy import`; migrate from a **tupperbox** (`tul!export` in dms) or **pluralkit** export; auto-links to wolves by name.\n_needs the **message content intent** plus **manage webhooks** + **manage messages** in the channel._\n\n**character identity**\n`/character`; set pronouns, birthday, birth sex, sexuality, maw belief, combat size, or age. pronouns default from lore or birth sex. ref image is admin-set via `/wolfadmin refimage`.\n`/family`; render a wolf's family tree / relationship web as a diagram.\n\n**body language**\n`/sign`; visual signals **alert · rally · play · submit · soothe · threaten · freeze · track · greet · grieve · challenge · read** (unlimited; repeat signs on the **same partner** within 30 min pay out less mood each time, down to 20%, so it can't replace `/playpen action:play`/`action:groom`). **npcs** use `/npc sign` (real mood/standing effects on the target's wolf; same diminishing returns). **mute** wolves can't `/howl`, so their **rally** sign builds extra unity instead; denmates answer with `/sign signal:read`. **freeze** also cuts ambush odds to 40% for 20 real minutes on hunt/explore/patrol.\n`/weep`; silverrush only, once per sunrise: release grief alone at the weep stone (**+10 mood**, clears grief if you have it).\n\n**scenes**\n`/scene start`; opens a thread titled **yourwolf + partner; location** (partner and location optional; location defaults to your `/location`). `topic:` is the opening blurb, not the thread name. `/scene join` · `/scene here` · `/scene poke` · `/scene leave` · `/scene end` (opener or admin). posting in an open scene **auto-joins** you. _needs **create public threads**._\n\n**quick ic (no proxy intent)**\n`/say`; one-line ic embed as your wolf. `/whisper`; styled ic dm to another player.\n`/location set` · `clear` · `show`; where your wolf is ic (shown on `/profile` and proxy footers).\n\n**journal**; `/journal` reads your wolf's **automatic** timeline; backfills from lore sheets, birth line, bonds, courtship, found families, blooding, and death records.\n\n**ceremonies**; `/rite naming` · `blooding` · `mourning`.\n\n**npcs**; admins `/npc add` · `remove` · `list`; anyone `/npc say` · `/npc sign` (real effects on a player wolf) · `/npc narrate` (admin; no named speaker).\n\nproxy ooc: wrap `((out of character))` or start a line with `//`.\n\n**birthdays**; wolves are celebrated automatically in the sunrise den news whenever they cross a full year (12 moons)."), 'admin': ('admin', "requires `admin_ids` in `.env` or server administrator.\n\n`/rollover`; advance the game day\n`/wolfadmin assign`; create a wolf for a player\n`/wolfadmin transfer`; move a wolf between players\n`/wolfadmin possess` / `release`; steer another player's wolf for rp\n`/wolfadmin list`; inspect a player's wolves\nage now set via `/character age_moons:`\n`/wolfadmin proxy`; set/clear/list a player's proxy tags or autoproxy (they set avatars with `/proxy avatar`)\n`/wolfadmin featurepending`; list pending bonus role-feature requests\n`/wolfadmin approvefeature`; approve a request (spends 10 xp)\n`/wolfadmin denyfeature`; deny a request (no xp spent)\n`/wolfadmin deaths`; death log and causes (optional player filter)\n\n`/wolfadmin dormant`; toggle a wolf's exemption from hunger/hydration/mood decay (admin-held npcs not actively played)\n`/wolfadmin execute`; mark a wolf dead with a lore-flavored cause (sog grave, high ledge, or custom)\n`/questadmin create`; post a custom quest\n`/questadmin list`; all board quests\n`/questadmin remove`; delete a quest\n`/patronadmin code`; create a donation redeem code\n`/patronadmin grant`; manually grant donor rewards\n`/patronadmin kickstarter grant`; kickstarter backer badge\n`/patronadmin kickstarter tier2`; tier 2 bundle (badge + 75 bones + item)\n`/patronadmin orders`; pending ko-fi shop orders\n`/patronadmin fulfill`; mark a shop order delivered"), 'credits': ('credits', CREDITS_TEXT)}
HELP_TOPIC_FOOTERS = {'overview': 'Try /help topic:getting-started · /checklist', 'getting-started': '/checklist · /help topic:hunting', 'economy': '/bones action:shop · /checklist', 'hunting': '/checklist · /help topic:wolvden', 'wolvden': '/explore · /scout survey · /playpen action:toys', 'skills': '/skills category:tracking · /skilllist · /rpg action:roll', 'pack': '/howl · /pack patrol · /help topic:roles', 'world': '/checklist · /world action:time', 'rpg': '/skills · /combat start · /help topic:skills', 'prestige': '/prestige action:view · /prestige action:require', 'roles': '/role action:event · /help topic:lore', 'lore': '/role action:prophecy · /medic action:sacred'}

class Help(commands.Cog):

    def __init__(self, bot: commands.Bot):
        self.bot = bot

    @app_commands.command(name='ping', description='bot health check; latency and uptime.')
    async def ping(self, interaction: discord.Interaction):
        latency_ms = round(self.bot.latency * 1000)
        embed = howlbert_embed(f'{BOT_DISPLAY_NAME} is awake', f'**Latency**; {latency_ms} ms\n**Guilds**; {len(self.bot.guilds)}', color=EMBED_COLOR)
        embed.set_footer(text=embed_footer('Use /help for commands'))
        await interaction.response.send_message(embed=embed, ephemeral=reply_ephemeral())

    @app_commands.command(name='help', description='learn how to use howlbert; wolf rp commands and den mechanics.')
    @app_commands.describe(topic='category to read about')
    @app_commands.choices(topic=[app_commands.Choice(name='overview', value='overview'), app_commands.Choice(name='getting started', value='getting-started'), app_commands.Choice(name='profile', value='profile'), app_commands.Choice(name='economy', value='economy'), app_commands.Choice(name='hunting', value='hunting'), app_commands.Choice(name='wolvden (explore & mood)', value='wolvden'), app_commands.Choice(name='unbelievaboat (economy)', value='unbelievaboat'), app_commands.Choice(name='whispering wild (fog spirits)', value='whispering'), app_commands.Choice(name='quests', value='quests'), app_commands.Choice(name='role features', value='roles'), app_commands.Choice(name='maw faith & oracles', value='lore'), app_commands.Choice(name='wolf tongue / terms', value='terms'), app_commands.Choice(name='pack & warfare', value='pack'), app_commands.Choice(name='world & rollover', value='world'), app_commands.Choice(name='prestige & legacy', value='prestige'), app_commands.Choice(name='rpg rolls', value='rpg'), app_commands.Choice(name='skill checks', value='skills'), app_commands.Choice(name='life & advancement', value='life'), app_commands.Choice(name='roleplay & proxying', value='roleplay'), app_commands.Choice(name='admin', value='admin'), app_commands.Choice(name='credits', value='credits')])
    async def help(self, interaction: discord.Interaction, topic: str | None=None):
        title, body = HELP_TOPICS.get(topic or 'overview', HELP_TOPICS['overview'])
        embed = howlbert_embed(title, body, color=EMBED_COLOR)
        footer_extra = HELP_TOPIC_FOOTERS.get(topic or 'overview')
        if footer_extra:
            embed.set_footer(text=embed_footer(footer_extra))
        await interaction.response.send_message(embed=embed, ephemeral=reply_ephemeral())

async def setup(bot: commands.Bot):
    await bot.add_cog(Help(bot))
