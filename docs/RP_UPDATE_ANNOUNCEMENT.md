# rp update announcement (discord paste)

post these **in order** as separate messages. only **part 1** needs `@everyone`. each block is under discord's 2000 character limit without nitro.

character counts are approximate; trim a line if discord complains.

---

## part 1 (ping)

```

```

---

## part 2 (proxy + tupperbox migration)

```
**speak as your wolf**

✦ `/proxy set tag:H:text` then type `H:your line` (howlbert reposts as that wolf and deletes your message)
✦ `/proxy avatar` upload and crop a portrait; `/proxy autoproxy` to default one wolf (`\` at the start of a line sends as you once)
✦ `((ooc))` or `//` in proxy text; ic posts as the wolf, ooc in the footer
✦ `/say` and `/whisper` for quick ic lines and private ic dms
✦ `/character` for pronouns, bio, birthday, avatar, ref on `/profile`

**migrating from tupperbox**

1. open dms with the tupperbox bot
2. send `tul!export`
3. save the `.json` file it sends you
4. in this server run `/proxy import` and attach that file
5. export names must match your howlbert wolf names (`/wolves` to check). if something didn't link, `/rename` to match, then `/proxy import` again
6. optional: `/proxy avatar` if you want cropped portraits here; `/proxy autoproxy` if you had autoproxy on before

staff need **message content intent** on in the developer portal + `ENABLE_MESSAGE_CONTENT_INTENT=true` in the bot `.env` for typed proxy tags to work. the bot also needs **manage webhooks** and **manage messages** in rp channels.
```

---

## part 3 (body language + scenes)


